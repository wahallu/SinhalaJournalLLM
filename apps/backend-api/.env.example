# Copy to .env and fill in real values. Never commit the actual .env.

# ── Database ──
# When running via infra/docker-compose.yml, host "db" is the compose
# service name. Point this at your real Postgres instance in production.
DATABASE_URL=postgresql+asyncpg://sinhalajournal:sinhalajournal@db:5432/sinhalajournalllm

# ── App ──
APP_ENV=development

# ── CORS (comma-separated origins) ──
CORS_ORIGINS=http://localhost:5173
