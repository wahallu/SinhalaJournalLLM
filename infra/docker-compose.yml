# Local development orchestration only.
#
# This is NOT the production deployment definition. Production deploys go
# through Coolify, one app at a time, each built from its own folder under
# apps/ (see infra/README.md) — that keeps apps independently deployable,
# which this file intentionally does not need to preserve for local dev.
#
# Usage (from repo root):
#   docker compose -f infra/docker-compose.yml up

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sinhalajournal}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sinhalajournal}
      POSTGRES_DB: ${POSTGRES_DB:-sinhalajournalllm}
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sinhalajournal}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend-api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ../apps/backend-api:/app
    command: sh -c "pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - ../apps/backend-api/.env
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  db-data:
